import { add, subtract, multiply, divide } from './math';

const result = add(10, 20);
console.log(result); // 30

const result = subtract(100, 50);
console.log(result); // 50

const result = multiply(10, 5);
console.log(result); // 50

const result = divide(100, 2);
console.log(result); // 50
